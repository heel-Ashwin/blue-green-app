# service.yaml (The traffic router)
apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 32000
  selector:
    app: myapp
    color: blue # Initial traffic directed to the BLUE environment